node_modules
dist
.env
.env.*

# Playwright
playwright-report/
test-results/
test-results-e2e/

# Capturas de tela para produção de vídeo (contêm dados pessoais)
.reels-capturas/

# Configuração local de ferramentas de IA/editor (não usada pelo projeto)
.qwen/

# Configuração local do editor Cursor
.cursor/

# Vínculo local com o projeto Vercel (não deve ser compartilhado)
.vercel/

# Pacote de extensão do VS Code adicionado acidentalmente ao repositório
Microsoft.VisualStudio.Services.VSIXPackage

# Backups e exportações do Firestore (contêm dados pessoais e operacionais)
backup-*.json
backup_*.json
backup_gestao_ferramentas*.json
pre_restore_*.json
pre_reset_*.json
firebase-data-export.json

# Logs de debug do Firebase Emulator (podem conter documentos usados nos testes)
*-debug.log
